export interface Caminhao {
  modelo: string;
  placa: string;
  comprimento: number; // em metros
  largura: number;     // em metros
  altura: number;      // em metros
  pesoLimite: number;  // em toneladas
}
